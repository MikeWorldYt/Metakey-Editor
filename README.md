
  # Create PyQt6 GUI Layout

  This is a code bundle for Create PyQt6 GUI Layout. The original project is available at https://www.figma.com/design/BrRty8XzOdzsbIUAzvZK65/Create-PyQt6-GUI-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  